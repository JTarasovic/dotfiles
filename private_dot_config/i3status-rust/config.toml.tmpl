# vi:syntax=toml

{{ $nc700 := output
    ( joinPath .chezmoi.sourceDir "helpers" "bluetooth-device-paired.sh")
    "Bose NC 700 Head" -}}

{{ $px7 := output
    ( joinPath .chezmoi.sourceDir "helpers" "bluetooth-device-paired.sh")
    "PX7 Bowers & Wilkins" -}}

[theme]
theme = "nord-dark"
[theme.overrides]
separator = ""

[icons]
icons = "material-nf"

[[block]]
block = "custom"
interval = "once"
command = ''' jq -n --arg k "$(uname -r)" '{"state": "Info", "text": $k}' '''
json = true

[[block]]
block = "custom"
interval = "once"
signal = 1
command = ''' current-context '''

[[block]]
block = "music"
format = " $icon {$combo.str(max_w:75,rot_interval:0.5) $play |}"

{{ if $nc700 -}}
# [[block]]
# block = "bluetooth"
# mac = {{ $nc700 | quote }}
# format = "Bose NC700 {percentage}"
# format_unavailable = "Bose NC700 x"
{{- else -}}
# skipped Bose NC700
{{- end }}

{{ if $px7 -}}
# [[block]]
# block = "bluetooth"
# mac = {{ $px7 | quote }}
# hide_disconnected = false
# format = " B&W PX7 {percentage}"
# format_unavailable = " B&W PX7 x"
{{- else -}}
# skipped PX7
{{- end }}

[[block]]
block = "sound"
headphones_indicator = true
show_volume_when_muted = true
format = " $icon {$output_name} {$volume} |"

[[block.click]]
button = "left"
cmd = {{ (joinPath .chezmoi.homeDir ".local" "bin" "toggle_sinks") | quote }}

[block.mappings]
"alsa_output.pci-0000_0e_00.4.analog-stereo" = "\uf025"
"alsa_output.pci-0000_0c_00.1.hdmi-stereo" = "\uf9c2"
"alsa_output.pci-0000_0c_00.1.hdmi-stereo-extra3" = "\uf9c2"

[[block]]
block = "sound"
device_kind = 'source'
show_volume_when_muted = true

[[block]]
block = "weather"
format = " $icon $weather $temp"
interval = 900

[block.service]
name = "openweathermap"
api_key = "{{ .openweathermap.api_key }}"
city_id = "4377664"
units = "imperial"

[[block]]
block = "time"
interval = 60

[block.format]
full = " $icon $timestamp.datetime(f:'%a %b %d %R') "
short = " $icon $timestamp.datetime(f:%R) "

[[block]]
block = "custom"
command = "browsers GET"
interval = "once"
signal = 2
[[block.click]]
button = "left"
cmd = "{{ (joinPath .chezmoi.homeDir ".local" "bin" "browsers") }} CYCLE"

[[block]]
block = "notify"

[[block]]
block = "custom"
command = "echo \uf011"
interval = "once"
[[block.click]]
button = "left"
cmd = {{ (joinPath .chezmoi.homeDir ".local" "bin" "powermenu") | quote}}
